services:
  - type: web
    name: movie-flask
    env: python
    buildCommand: |
      pip install -r requirements.txt
      cd src
      g++ -std=c++17 main.cpp -o program
      chmod +x program
    startCommand: |
      cd src
      python server.py